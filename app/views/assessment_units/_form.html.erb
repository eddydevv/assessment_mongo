<%= simple_form_for(@assessment_unit) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.association :year %>
    <%= f.association :assessment_subtype %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
